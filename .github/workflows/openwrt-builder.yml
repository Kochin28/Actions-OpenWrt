name: CT3003_Final_Success_V3

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: master
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialization environment
      run: |
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install build-essential git
        sudo timedatectl set-timezone "$TZ"

    - name: Clone source code
      run: git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt

    - name: Update & Install Feeds
      run: |
        cd openwrt
        # 只更新官方源，唔喺度加 PassWall，防止 Feeds 報錯
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Download PassWall 2 Directly
      run: |
        cd openwrt/package
        # 暴力做法：直接 clone 到 package 目錄，跳過 Feeds 合併過程
        git clone --depth 1 https://github.com/xiaorouji/openwrt-passwall-packages.git pw_pkgs
        git clone --depth 1 https://github.com/xiaorouji/openwrt-passwall.git pw_app
        # 清理可能衝突嘅包
        rm -rf pw_pkgs/v2ray-geodata
        rm -rf pw_pkgs/mosdns

    - name: Load Custom Configuration
      run: |
        cd openwrt
        # 1. 修改默認 IP 為 192.168.6.1
        sed -i 's/192.168.1.1/192.168.6.1/g' package/base-files/files/bin/config_generate
        
        # 2. 寫入 CT3003 核心配置 (針對 256M 內存精簡)
        cat >> .config <<EOF
        CONFIG_TARGET_mediatek=y
        CONFIG_TARGET_mediatek_filogic=y
        CONFIG_TARGET_mediatek_filogic_DEVICE_cetron_ct3003=y
        
        # 核心插件：PassWall 2 + SSR Plus + TurboACC
        CONFIG_PACKAGE_luci-app-passwall=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
        CONFIG_PACKAGE_luci-app-ssr-plus=y
        CONFIG_PACKAGE_luci-app-turboacc=y
        CONFIG_PACKAGE_luci-app-ramfree=y
        CONFIG_LUCI_LANG_zh_Hans=y
        
        # 禁用 IPv6 慳內存
        # CONFIG_PACKAGE_luci-proto-ipv6 is not set
        # CONFIG_PACKAGE_kmod-ipv6 is not set
        EOF
        
        make defconfig

    - name: Final Compile
      run: |
        cd openwrt
        # 第一次編譯用單線程 V=s，方便睇錯喺邊
        make -j1 V=s

    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: CT3003_Final_Firmware
        path: openwrt/bin/targets/mediatek/filogic/*.bin
